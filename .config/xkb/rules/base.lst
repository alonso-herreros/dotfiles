; Replace the 'custom' entries at the end of the file, under the '! option' section.
; If the last section is not the '! option' section, find it and add these lines there.
; If there's no such section, add a `! option` line and these lines under it.

  altgreek             Access the greek alphabet using AltGr
  altgreek:mathvar     Letter variants that are more common in math
  altmath              Access math symbols using AltGr
  altmath:sub_comma    Superscript dead key at AltGr+,
  altmath:sup_dot      Subscript dead key at AltGr+.
  undead               Layers to remove or specify dead keys
  undead:es_only       Make non-spanish keys not dead
  custom               Miscellaneous options
  custom:types         Use user-defined custom XKB types
  custom:test          Option for testing


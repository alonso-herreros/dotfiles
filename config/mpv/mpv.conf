# ==========  SCALING  =====================================

# Scale large media files to your display's height and keep the aspect ratio.
autofit-larger=100%x100%


# ==========  LOOPING  =====================================

# alias for loop-file
#loop

# Instead of closing after a video/image finishes, keep repeating it.
# Choices: no inf yes (or an integer) (0 to 10000) (default: no)
loop-file=inf

# When you reach the start or end of a directory / playlist, loop back around.
# Choices: no inf yes force (or an integer) (1 to 10000) (default: no)
loop-playlist=no


# ==========  DIR SCANNING  ================================

# Allow going to the next / previous media file in the current directory even
# if you open an individual file (such as opening a file with a file explorer).
# Choices: no filter same (default: no)
autocreate-playlist=same

# Also don't recurse into directories, "ignore" locks it to the current directory.
# Choices: auto lazy recursive ignore (default: auto)
directory-mode=ignore

# String list (default: video,audio,image,archive,playlist)
#directory-filter-types


# ==========  MISC  ========================================

# Clear a few common properties when moving between media files. This list can
# grow very large (hue, saturation, etc.), let's start with this for now.
reset-on-next-file=window-scale,video-zoom,video-rotate,video-align-x,video-align-y,video-aspect-override,video-pan-x,video-pan-y,panscan


# ==========  UNKNOWN  =====================================

# Image options? Not sure if this would even do anything
# # mpv is really good at viewing images, not just videos!
# #
# # vo=x11 disables hardware acceleration for images which helps prevent GPU
# # memory from being heavily consumed in Wayland. wlshm is not being used because
# # this shares memory with niri and niri never releases this memory back.
# #
# # force-window=no ensures the initial image opened is the correct size. It's not
# # needed with x11 but if wlshm gets addressed it is needed.
# [image-settings]
# profile-cond=p["current-tracks/video/image"]
# vo=x11
# #vo=wlshm
# force-window=no


# ================================================================ #
#                   ALL OPTIONS                                    #
# ================================================================ #

# Time (default: no)
#ab-loop-a

# Time (default: no)
#ab-loop-b

# Choices: inf (or an integer) (0 to 2147483647) (default: inf)
#ab-loop-count

# Flag (default: yes)
#access-references

# String (default: )
#ad

# Float (0 to 6) (default: 0)
#ad-lavc-ac3drc

# Flag (default: no)
#ad-lavc-downmix

# Key/value list (default: )
#ad-lavc-o

# Integer (0 to 16) (default: 1)
#ad-lavc-threads

# Flag (default: no)
#ad-queue-enable

# ByteSize (0 to 4.6116860184274e+18) (default: 1.000 MiB)
#ad-queue-max-bytes

# Integer64 (0 to any) (default: 48000)
#ad-queue-max-samples

# Double (0 to any) (default: 1)
#ad-queue-max-secs

# Object settings list (default: )
#af

# Choices: no auto (or an integer) (0 to 8190) (default: auto)
#aid

# String list (default: )
#alang

# Flag (default: no)
#allow-delayed-peak-detect

# Integer (0 to 2147483647) (default: 100000)
#alsa-buffer-time

# Flag (default: no)
#alsa-ignore-chmap

# String (default: default)
#alsa-mixer-device

# Integer (0 to 99) (default: 0)
#alsa-mixer-index

# String (default: Master)
#alsa-mixer-name

# Flag (default: no)
#alsa-non-interleaved

# Integer (0 to 2147483647) (default: 4)
#alsa-periods

# Flag (default: no)
#alsa-resample

#
#ao add append clr help pre set toggle remove

# Flag [ Object settings list (default: ) ] (default: no)
#ao-null-broken-delay

# Flag (default: no)
#ao-null-broken-eof

# Float (0 to 100) (default: 0.2)
#ao-null-buffer

# Audio channels or channel map (default: )
#ao-null-channel-layouts

# Audio format (default: no)
#ao-null-format

# Float (0 to 100) (default: 0)
#ao-null-latency

# Integer (1 to 100000) (default: 256)
#ao-null-outburst

# Float (0 to 10000) (default: 1)
#ao-null-speed

# Flag (default: no)
#ao-null-untimed

# Flag (default: no)
#ao-pcm-append

# String (default: ) [file]
#ao-pcm-file

# Flag (default: yes)
#ao-pcm-waveheader

# String list (default: zip,rar,7z,cbz,cbr)
#archive-exts

# alias for aid
#audio

# Integer (0 to 1024) (default: 10)
#audio-backward-batch

# Choices: auto (or an integer) (0 to 1024) (default: auto)
#audio-backward-overlap

# Double (0 to 10) (default: 0.2)
#audio-buffer

# Audio channels or channel map (default: auto-safe)
#audio-channels

# String (default: mpv)
#audio-client-name

# Float (default: 0)
#audio-delay

# String (default: )
#audio-demuxer

# String (default: auto)
#audio-device

# Choices: no embedded-first external-first (default: embedded-first)
#audio-display

# Flag (default: no)
#audio-exclusive

# String list (default: aac,ac3,aiff,ape,au,dts,eac3,flac,m4a,mka,mp3,oga,ogg,
# ogm,opus,thd,wav,wma,wv)
#audio-exts

# Flag (default: no)
#audio-fallback-to-null

# alias for --audio-files-append (CLI/config files only)
#audio-file

# Choices: no exact fuzzy all (default: no)
#audio-file-auto

# alias for audio-exts
#audio-file-auto-exts

# String list
#audio-file-paths

# String list
#audio-files

# Audio format (default: no)
#audio-format

# Flag (default: no)
#audio-normalize-downmix

# Flag (default: yes)
#audio-pitch-correction

# Double (0 to 1) (default: 0)
#audio-resample-cutoff

# Integer (0 to 32) (default: 16)
#audio-resample-filter-size

# Flag (default: no)
#audio-resample-linear
#audio-resample-max-output-size Double (default: 40)

# Integer (0 to 30) (default: 10)
#audio-resample-phase-shift

# ByteSize (0 to 4.6116860184274e+18) (default: 64.000 MiB)
#audio-reversal-buffer

# Integer (0 to 768000) (default: 0)
#audio-samplerate

# Flag (default: no)
#audio-set-media-role

# String (default: )
#audio-spdif

# Flag (default: no)
#audio-stream-silence

# Key/value list (default: )
#audio-swresample-o

# Float (0 to 60) (default: 0)
#audio-wait-open

# Flag (default: yes)
#auto-window-resize

# Window size (default: )
#autofit

# Window size (default: )
#autofit-larger

# Window size (default: )
#autofit-smaller

# Flag (default: yes)
#autoload-files

# Choices: no (or an integer) (0 to 10000) (default: 0)
#autosync

# Choices: none color tiles (default: tiles)
#background

# Float (default: 16)
#background-blur-radius

# Color (default: #FF000000)
#background-color

# Color (default: #FFEDEDED)
#background-tile-color-0

# Color (default: #FFDEDEDE)
#background-tile-color-1

# Integer (1 to 4096) (default: 16)
#background-tile-size

# Choices: no yes video (default: no)
#blend-subtitles

# Integer (1 to 999) (default: 1)
#bluray-angle

# String (default: ) [file]
#bluray-device

# Flag (default: yes)
#border

# Choices: none color tiles blur (default: color)
#border-background

# Float (-100 to 100) (default: 0)
#brightness

# Choices: no auto yes (default: auto)
#cache

# Flag (default: no)
#cache-on-disk

# Flag (default: yes)
#cache-pause

# Flag (default: no)
#cache-pause-initial

# Float (0 to 3.4028234663853e+38) (default: 1)
#cache-pause-wait

# Double (0 to any) (default: 3600000)
#cache-secs

# Flag (default: no)
#cdda-cdtext

# String (default: ) [file]
#cdda-device

# Integer (0 to 75) (default: -1)
#cdda-overlap

# Integer (0 to 2) (default: 0)
#cdda-paranoia

# Integer (1 to 100) (default: 0)
#cdda-sector-size

# Flag (default: yes)
#cdda-skip

# Integer (default: 0) [deprecated]
#cdda-span-a

# Integer (default: 0) [deprecated]
#cdda-span-b

# Integer (1 to 100) (default: 0)
#cdda-speed

# Integer (default: 0)
#cdda-toc-offset

# Integer (0 to 10000) (default: 100)
#chapter-merge-threshold

# Double (default: 5)
#chapter-seek-threshold

# String (default: ) [file]
#chapters-file

# Object settings list (default: win32,mac,wayland,x11,vo)
#clipboard-backends

# Flag (default: no)
#clipboard-monitor

# Flag (default: no)
#clipboard-xwayland

# Flag (default: yes)
#config

# Double (0 to any) (default: 0)
#container-fps-override

# Float (-100 to 100) (default: 0)
#contrast

# Flag (default: no)
#cookies

# String (default: ) [file]
#cookies-file

# Float (0 to 1) (default: 0)
#corner-rounding

# Flag (default: yes)
#correct-downscaling

# Flag (default: yes)
#correct-pts

# Choices: no exact fuzzy all (default: exact)
#cover-art-auto

# alias for image-exts
#cover-art-auto-exts

# alias for --cover-art-files-append (CLI/config files only)
#cover-art-file

# String list (default: ) [file]
#cover-art-files

# String list (default: AlbumArt,Album,cover,front,AlbumArtSmall,Folder,.folder,thumb)
#cover-art-whitelist

# bilinear bicubic_fast oversample spline16 spline36 spline64 sinc lanczos ginseng bicubic hermite catmull_rom mitchell robidoux robidouxsharp box nearest triangle gaussian jinc ewa_lanczos ewa_hanning ewa_ginseng ewa_lanczossharp ewa_lanczos4sharpest ewa_lanczossoft haasnsoft ewa_robidoux ewa_robidouxsharp bartlett cosine hanning tukey hamming quadric welch kaiser blackman sphinx (default: )
#cscale                         Choices:

# Float (0 to 1) (default: 0)
#cscale-antiring

# Float (default: 0)
#cscale-blur

# Float (0 to 1) (default: 0)
#cscale-clamp

# Float (default: default)
#cscale-param1

# Float (default: default)
#cscale-param2

# Float (0.5 to 16) (default: 0)
#cscale-radius

# Float (0 to 1) (default: 0)
#cscale-taper

# bartlett cosine hanning tukey hamming quadric welch kaiser blackman sphinx jinc (default: )
#cscale-window                  Choices:

# Float (default: default)
#cscale-wparam

# Float (0 to 1) (default: 0)
#cscale-wtaper

# Choices: auto (or an integer) (0 to 2147483647) (default: auto)
#cuda-decode-device

# Choices: no always (or an integer) (0 to 30000) (default: 1000)
#cursor-autohide

# Flag (default: no)
#cursor-autohide-fs-only

# Flag (default: no)
#deband

# Float (0 to 4096) (default: 32)
#deband-grain

# Integer (0 to 16) (default: 1)
#deband-iterations

# Float (1 to 64) (default: 16)
#deband-range

# Float (0 to 4096) (default: 48)
#deband-threshold

# Choices: no yes auto (default: no)
#deinterlace

# Choices: tff bff auto (default: auto)
#deinterlace-field-parity

# String (default: )
#demuxer
#demuxer-backward-playback-step Double (0 to any) (default: 60)

# String (default: ) [file]
#demuxer-cache-dir

# Choices: immediate whendone no (default: immediate)
#demuxer-cache-unlink-files

# Flag (default: no)
#demuxer-cache-wait

# Flag (default: yes)
#demuxer-donate-buffer

# Double (0 to any) (default: 0)
#demuxer-hysteresis-secs

# Flag (default: yes)
#demuxer-lavf-allow-mimetype

# Float (0 to 3600) (default: 0)
#demuxer-lavf-analyzeduration

# Integer (1 to 10485760) (default: 32768)
#demuxer-lavf-buffersize

# String (default: )
#demuxer-lavf-format

# Flag (default: yes)
#demuxer-lavf-hacks
#demuxer-lavf-linearize-timestamps Choices: no auto yes (default: auto)

# Key/value list (default: )
#demuxer-lavf-o

# Choices: no yes auto nostreams (default: auto)
#demuxer-lavf-probe-info

# Integer (1 to 100) (default: 26)
#demuxer-lavf-probescore

# Integer (32 to 2147483647) (default: 0)
#demuxer-lavf-probesize

# Flag (default: yes)
#demuxer-lavf-propagate-opts

# ByteSize (0 to 4.6116860184274e+18) (default: 50.000 MiB)
#demuxer-max-back-bytes

# ByteSize (0 to 4.6116860184274e+18) (default: 150.000 MiB)
#demuxer-max-bytes

# Flag (default: yes)
#demuxer-mkv-crop-compat

# Flag (default: yes)
#demuxer-mkv-probe-start-time
#demuxer-mkv-probe-video-duration Choices: no yes full (default: no)

# Choices: no yes index (default: index)
#demuxer-mkv-subtitle-preroll
#demuxer-mkv-subtitle-preroll-secs Double (0 to any) (default: 1)
#demuxer-mkv-subtitle-preroll-secs-index Double (0 to any) (default: 10)

# Audio channels or channel map (default: stereo)
#demuxer-rawaudio-channels

# Choices: u8 s8 u16le u16be s16le s16be u24le u24be s24le s24be u32le u32be s32le s32be floatle floatbe doublele doublebe u16 s16 u24 s24 u32 s32 float double (default: s16le)
#demuxer-rawaudio-format

# Integer (1000 to 384000) (default: 44100)
#demuxer-rawaudio-rate

# String (default: )
#demuxer-rawvideo-codec

# FourCC (default: 30323449)
#demuxer-rawvideo-format

# Float (0.001 to 1000) (default: 25)
#demuxer-rawvideo-fps

# Integer (1 to 8192) (default: 720)
#demuxer-rawvideo-h

# Image format (default: no)
#demuxer-rawvideo-mp-format

# Integer (1 to 268435456) (default: 0)
#demuxer-rawvideo-size

# Integer (1 to 8192) (default: 1280)
#demuxer-rawvideo-w

# Double (0 to any) (default: 1)
#demuxer-readahead-secs

# Choices: auto no yes (default: auto)
#demuxer-seekable-cache

# Double (default: 0.1)
#demuxer-termination-timeout

# Flag (default: yes)
#demuxer-thread

# Double (0 to any) (default: 0)
#display-fps-override

# String list (default: Artist,Album,Album_Artist,Comment,Composer,Date,Description,Genre,Performer,Rating,Series,Title,Track,icy-title,service_name,Uploader,Channel_URL)
#display-tags

# Choices: fruit ordered error-diffusion no (default: fruit)
#dither

# Choices: no auto (or an integer) (-1 to 16) (default: auto)
#dither-depth

# Integer (2 to 8) (default: 6)
#dither-size-fruit

# Choices: no auto replace append insert-next (default: auto)
#drag-and-drop

# String (default: )
#drm-connector

# String (default: ) [file]
#drm-device

# Choices: primary overlay (or an integer) (0 to 2147483647) (default: primary)
#drm-draw-plane

# Window size (default: )
#drm-draw-surface-size

# Choices: primary overlay (or an integer) (0 to 2147483647) (default: overlay)
#drm-drmprime-video-plane

# Choices: xrgb8888 xrgb2101010 xbgr8888 xbgr2101010 yuyv (default: xrgb8888)
#drm-format

# String (default: preferred)
#drm-mode

# Choices: no yes auto (default: no)
#drm-vrr-enabled

# bilinear bicubic_fast oversample spline16 spline36 spline64 sinc lanczos ginseng bicubic hermite catmull_rom mitchell robidoux robidouxsharp box nearest triangle gaussian jinc ewa_lanczos ewa_hanning ewa_ginseng ewa_lanczossharp ewa_lanczos4sharpest ewa_lanczossoft haasnsoft ewa_robidoux ewa_robidouxsharp bartlett cosine hanning tukey hamming quadric welch kaiser blackman sphinx (default: hermite)
#dscale                         Choices:

# Float (0 to 1) (default: 0)
#dscale-antiring

# Float (default: 0)
#dscale-blur

# Float (0 to 1) (default: 0)
#dscale-clamp

# Float (default: default)
#dscale-param1

# Float (default: default)
#dscale-param2

# Float (0.5 to 16) (default: 0)
#dscale-radius

# Float (0 to 1) (default: 0)
#dscale-taper

# bartlett cosine hanning tukey hamming quadric welch kaiser blackman sphinx jinc (default: )
#dscale-window                  Choices:

# Float (default: default)
#dscale-wparam

# Float (0 to 1) (default: 0)
#dscale-wtaper

# String (default: ) [file]
#dump-stats

# Integer (0 to 15) (default: 0)
#dvbin-card

# Integer (default: 0)
#dvbin-channel-switch-offset

# String (default: ) [file]
#dvbin-file

# Flag (default: no)
#dvbin-full-transponder

# String (default: )
#dvbin-prog

# Float (0 to 3.4028234663853e+38) (default: 30)
#dvbin-timeout

# Integer (1 to 99) (default: 1)
#dvd-angle

# String (default: ) [file]
#dvd-device

# Integer (default: 0)
#dvd-speed

# Choices: auto (or an integer) (0 to 8190) (default: auto)
#edition

# Integer (default: 0)
#egl-config-id

# Choices: auto rgb8 rgba8 rgb10 rgb10_a2 rgb16 rgba16 rgb16f rgba16f rgb32f rgba32f (default: auto)
#egl-output-format

# Flag (default: yes)
#embeddedfonts

# Relative time or percent position (default: none)
#end

# String (default: sierra-lite)
#error-diffusion

# alias for --external-files-append (CLI/config files only)
#external-file

# String list (default: ) [file]
#external-files

# String (default: auto)
#fbo-format

# Choices: never open all (default: open)
#focus-on

# removed [deprecated]
#focus-on-open

# String (default: )
#force-media-title

# Flag (default: no)
#force-render

# Flag (default: no)
#force-rgba-osd-rendering

# Flag (default: no)
#force-seekable

# Choices: no yes immediate (default: no)
#force-window

# Flag (default: no)
#force-window-position

# Choices: no vo decoder decoder+vo (default: vo)
#framedrop

# Choices: all (or an integer) (0 to 2147483647) (default: all)
#frames

# alias for fullscreen
#fs

# Choices: all current (or an integer) (0 to 32) (default: current)
#fs-screen

# String (default: )
#fs-screen-name

# Flag (default: no)
#fullscreen

# Float (-100 to 100) (default: 0)
#gamma

# Flag (default: no) [deprecated]
#gamma-auto

# Float (0.1 to 2) (default: 1)
#gamma-factor

# removed [deprecated]
#gamut-clipping

# Choices: auto clip perceptual relative saturation absolute desaturate darken warn linear (default: auto)
#gamut-mapping-mode

# removed [deprecated]
#gamut-warning

# Choices: no yes weak (default: weak)
#gapless-audio

# Window geometry (default: )
#geometry

# alias for --glsl-shaders-append (CLI/config files only)
#glsl-shader

# Key/value list (default: )
#glsl-shader-opts

# String list (default: ) [file]
#glsl-shaders

# Object settings list (default: )
#gpu-api

# Object settings list (default: )
#gpu-context

# Flag (default: no)
#gpu-debug

# Choices: auto yes no (default: auto)
#gpu-dumb-mode

# String (default: auto)
#gpu-hwdec-interop

# Flag (default: yes)
#gpu-shader-cache

# String (default: ) [file]
#gpu-shader-cache-dir

# Flag (default: no)
#gpu-sw

# Integer (0 to 4096) (default: 0)
#gpu-tex-pad-x

# Integer (0 to 4096) (default: 0)
#gpu-tex-pad-y

# Choices: auto yes no (default: auto)
#hdr-compute-peak

# Float (0 to 2) (default: 0)
#hdr-contrast-recovery

# Float (1 to 100) (default: 3.5)
#hdr-contrast-smoothness

# Float (0 to 1000) (default: 20)
#hdr-peak-decay-rate

# Float (0 to 100) (default: 0)
#hdr-peak-percentile

# Choices: auto (or an integer) (10 to 10000) (default: auto)
#hdr-reference-white

# Float (0 to 20) (default: 3)
#hdr-scene-threshold-high

# Float (0 to 20) (default: 1)
#hdr-scene-threshold-low

# Flag (default: no)
#hidpi-window-scale

# Choices: no min max (or an integer) (0 to 2147483647) (default: max)
#hls-bitrate

# Choices: no absolute yes always default (default: default)
#hr-seek

# Float (default: 0)
#hr-seek-demuxer-offset

# Flag (default: yes)
#hr-seek-framedrop

# String list (default: )
#http-header-fields

# String (default: )
#http-proxy

# Float (-100 to 100) (default: 0)
#hue

# String list (default: no)
#hwdec

# String (default: h264,vc1,hevc,vp8,vp9,av1,prores,prores_raw,ffv1,dpx)
#hwdec-codecs

# Integer (0 to 256) (default: 6)
#hwdec-extra-frames

# Image format (default: no)
#hwdec-image-format

# Choices: no yes (or an integer) (1 to 2147483647) (default: 3)
#hwdec-software-fallback

# Integer (0 to any) (default: 4)
#hwdec-threads

# String (default: auto)
#icc-3dlut-size

# Flag (default: yes)
#icc-cache

# String (default: ) [file]
#icc-cache-dir

# Choices: no inf (or an integer) (0 to 1000000) (default: no)
#icc-force-contrast

# Integer (default: 1)
#icc-intent

# String (default: ) [file]
#icc-profile

# Flag (default: no)
#icc-profile-auto

# Flag (default: no)
#icc-use-luma

# Choices: no once yes (default: no)
#idle
#ignore-path-in-watch-later-config Flag (default: no)

# Double (0 to inf) (default: 5)
#image-display-duration

# String list (default: avif,bmp,gif,heic,heif,j2k,jp2,jpeg,jpg,jxl,png,qoi,svg,tga,tif,tiff,webp)
#image-exts

# String (default: ) [file]
#image-lut

# Choices: auto native normalized conversion (default: auto)
#image-lut-type

# Choices: sdr video (or an integer) (10 to 10000) (default: sdr)
#image-subs-hdr-peak

# Flag (default: no)
#image-subs-video-resolution

# String (default: ) [file]
#include

# Choices: default recreate (default: default)
#index

# Flag (default: yes)
#initial-audio-sync

# Integer (default: 200)
#input-ar-delay

# Integer (default: 40)
#input-ar-rate

# Flag (default: yes)
#input-builtin-bindings

# Flag (default: yes)
#input-builtin-dragging

# String list (default: )
#input-commands

# String (default: ) [file]
#input-conf

# Flag (default: yes)
#input-cursor

# Flag (default: no)
#input-cursor-passthrough

# Flag (default: yes)
#input-default-bindings

# Integer (0 to 1000) (default: 300)
#input-doubleclick-time

# Integer (default: 3)
#input-dragging-deadzone

# Flag (default: no)
#input-gamepad

# Flag (default: no)
#input-ime

# String (default: )
#input-ipc-client

# String (default: ) [file]
#input-ipc-server

# Integer (2 to 65000) (default: 7)
#input-key-fifo-size

# Flag (default: yes)
#input-media-keys

# Flag (default: yes)
#input-preprocess-wheel

# Flag (default: yes)
#input-right-alt-gr

# Flag (default: yes)
#input-tablet-emulate-mouse

# Flag (default: yes)
#input-terminal

# Flag (default: no)
#input-test

# Flag (default: yes)
#input-touch-emulate-mouse

# Flag (default: yes)
#input-vo-keyboard

# Flag (default: no)
#interpolation

# Flag (default: yes)
#interpolation-preserve

# Float (default: 0.01)
#interpolation-threshold

# Flag (default: no)
#inverse-tone-mapping

# Flag (default: no)
#jack-autostart

# Flag (default: yes)
#jack-connect

# String (default: mpv)
#jack-name

# String (default: )
#jack-port

# Choices: waveext any (default: waveext)
#jack-std-channel-layout

# Flag (default: no)
#js-memory-report

# Choices: no yes always (default: no)
#keep-open

# Flag (default: yes)
#keep-open-pause

# Flag (default: yes)
#keepaspect

# Flag (default: yes)
#keepaspect-window

# String (default: )
#lavfi-complex

# Relative time or percent position (default: none)
#length

# Key/value list (default: )
#libplacebo-opts

# Flag (default: yes)
#linear-downscaling

# Flag (default: no)
#linear-upscaling

# Choices: no yes auto (default: auto)
#load-auto-profiles

# Flag (default: yes)
#load-commands

# Flag (default: yes)
#load-console

# Flag (default: yes)
#load-context-menu

# alias [deprecated] for load-console
#load-osd-console

# Flag (default: yes)
#load-positioning

# Flag (default: yes)
#load-scripts

# Flag (default: yes)
#load-select

# Flag (default: yes)
#load-stats-overlay

# Flag (default: no)
#load-unsafe-playlists

# String (default: ) [file]
#log-file

# String (default: ) [file]
#lut

# Choices: auto native normalized conversion (default: auto)
#lut-type

# Float (0 to 100) (default: -1)
#mc

# Flag (default: yes)
#media-controls

# Flag (default: no)
#merge-files

# String (default: auto)
#metadata-codepage

# Double (default: 1)
#mf-fps

# String (default: )
#mf-type

# Float (0 to 9) (default: 0)
#monitoraspect

# Float (0.03125 to 32) (default: 1)
#monitorpixelaspect

# Flag (default: yes)
#msg-color

# Output verbosity levels (default: )
#msg-level

# Flag (default: no)
#msg-module

# Flag (default: no)
#msg-time

# Flag (default: no)
#mute

# Flag (default: yes)
#native-fs

# Flag (default: no)
#native-keyrepeat

# Flag (default: no)
#native-touch

# Double (0 to any) (default: 60)
#network-timeout

# String (default: )
#oac

# Key/value list (default: )
#oacopts

# Flag (default: yes)
#ocopy-metadata

# String (default: )
#of

# Key/value list (default: )
#ofopts

# Flag (default: no)
#on-all-workspaces

# Flag (default: no)
#ontop

# Choices: window system desktop (or an integer) (0 to 2147483647) (default: window)
#ontop-level

# Flag (default: yes)
#openal-direct-channels

# Integer (2 to 128) (default: 4)
#openal-num-buffers

# Integer (256 to 32768) (default: 8192)
#openal-num-samples

# Integer (default: 0)
#opengl-check-pattern-a

# Integer (default: 0)
#opengl-check-pattern-b

# Choices: no yes auto (default: no)
#opengl-early-flush

# Choices: auto yes no (default: auto)
#opengl-es

# Flag (default: no)
#opengl-glfinish

# Flag (default: no)
#opengl-pbo

# Flag (default: no)
#opengl-rectangle-textures

# Integer (default: 1)
#opengl-swapinterval

# Flag (default: no)
#opengl-waitvsync

# Flag (default: no)
#orawts

# Flag (default: yes)
#ordered-chapters

# String (default: ) [file]
#ordered-chapters-files

# String list (default: )
#oremove-metadata

# Flag (default: yes)
#osc

# Choices: left center right (default: left)
#osd-align-x

# Choices: top center bottom (default: top)
#osd-align-y

# Color (default: #AF000000)
#osd-back-color

# Flag (default: yes)
#osd-bar

# Float (-1 to 1) (default: 0)
#osd-bar-align-x

# Float (-1 to 1) (default: 0.5)
#osd-bar-align-y

# alias for osd-bar-outline-size
#osd-bar-border-size

# Float (0.1 to 50) (default: 3.125)
#osd-bar-h

# Float (0 to 1000) (default: 1.6)
#osd-bar-marker-min-size

# Float (0 to 100) (default: 1.3)
#osd-bar-marker-scale

# Choices: none triangle line (default: triangle)
#osd-bar-marker-style

# Float (0 to 1000) (default: 0.5)
#osd-bar-outline-size

# Float (1 to 100) (default: 75)
#osd-bar-w

# Integer (default: 0)
#osd-bitmap-max-size

# Float (0 to 20) (default: 0)
#osd-blur

# Flag (default: no)
#osd-bold

# alias for osd-outline-color
#osd-border-color

# alias for osd-outline-size
#osd-border-size

# Choices: outline-and-shadow opaque-box background-box (default: outline-and-shadow)
#osd-border-style

# Color (default: #FFFFFFFF)
#osd-color

# Integer (0 to 3600000) (default: 1000)
#osd-duration

# String (default: sans-serif)
#osd-font

# Choices: auto none fontconfig (default: auto)
#osd-font-provider

# Float (1 to 9000) (default: 30)
#osd-font-size

# String (default: ) [file]
#osd-fonts-dir

# Flag (default: no)
#osd-fractions

# Integer (default: 0)
#osd-glyph-limit

# Flag (default: no)
#osd-italic

# Choices: auto left center right (default: auto)
#osd-justify

# Choices: 0 1 2 3 (default: 1)
#osd-level

# Integer (0 to 2147483647) (default: 16)
#osd-margin-x

# Integer (0 to 2147483647) (default: 16)
#osd-margin-y

# String (default: )
#osd-msg1

# String (default: )
#osd-msg2

# String (default: )
#osd-msg3

# Choices: no bar msg msg-bar (default: bar)
#osd-on-seek

# Color (default: #FF000000)
#osd-outline-color

# Float (default: 1.65)
#osd-outline-size

# String (default: )
#osd-playing-msg

# Integer (0 to 3600000) (default: 0)
#osd-playing-msg-duration

# Choices: title filename both (default: title)
#osd-playlist-entry

# Double (-1 to 10000) (default: -1)
#osd-prune-delay

# Float (0 to 100) (default: 1)
#osd-scale

# Flag (default: yes)
#osd-scale-by-window

# Color (default: #FFFABD2F)
#osd-selected-color

# Color (default: #FF000000)
#osd-selected-outline-color

# alias for osd-back-color
#osd-shadow-color

# Float (default: 0)
#osd-shadow-offset

# Choices: simple complex (default: complex)
#osd-shaper

# Float (-10 to 10) (default: 0)
#osd-spacing

# String (default: )
#osd-status-msg

# Key/value list (default: )
#oset-metadata

# String (default: )
#ovc

# Key/value list (default: )
#ovcopts

# alias [deprecated] for display-fps-override
#override-display-fps

# Float (0 to 1) (default: 0)
#panscan

# Flag (default: no)
#pause

# Choices: native (or an integer) (1 to 2000) (default: native)
#pipewire-buffer

# String (default: )
#pipewire-remote

# Choices: channel global (default: channel)
#pipewire-volume-mode

# Double (0.01 to 100) (default: 1)
#pitch

# alias [deprecated] for play-direction
#play-dir

# Choices: forward + backward - (default: forward)
#play-direction

# Choices: cplayer pseudo-gui (default: cplayer)
#player-operation-mode

# String list (default: cue,edl,m3u,m3u8,pls)
#playlist-exts

# Choices: auto no (or an integer) (0 to 2147483647) (default: auto)
#playlist-start

# Flag (default: no)
#prefetch-playlist

# String list (default: )
#profile

# Flag (default: no)
#pulse-allow-suspended

# Choices: native (or an integer) (1 to 2000) (default: 100)
#pulse-buffer

# String (default: )
#pulse-host

# Flag (default: yes)
#pulse-latency-hacks

# Flag (default: no)
#quiet

# Flag (default: no)
#rar-list-all-volumes

# Flag (default: no)
#really-quiet

# Flag (default: yes)
#rebase-start-time

# String (default: )
#referrer

# Choices: no track album (default: no)
#replaygain

# Flag (default: no)
#replaygain-clip

# Float (-200 to 60) (default: 0)
#replaygain-fallback

# Float (-150 to 150) (default: 0)
#replaygain-preamp

# String list (default: )
#reset-on-next-file

# Flag (default: yes)
#resume-playback

# Flag (default: no)
#resume-playback-check-mtime

# Choices: lavf udp tcp http udp_multicast (default: tcp)
#rtsp-transport

# Float (-100 to 100) (default: 0)
#saturation

# Flag (default: no)
#save-position-on-quit

# Flag (default: no)
#save-watch-history

# Choices: bilinear bicubic_fast oversample spline16 spline36 spline64 sinc lanczos ginseng bicubic hermite catmull_rom mitchell robidoux robidouxsharp box nearest triangle gaussian jinc ewa_lanczos ewa_hanning ewa_ginseng ewa_lanczossharp ewa_lanczos4sharpest ewa_lanczossoft haasnsoft ewa_robidoux ewa_robidouxsharp bartlett cosine hanning tukey hamming quadric welch kaiser blackman sphinx (default: lanczos)
#scale

# Float (0 to 1) (default: 0)
#scale-antiring

# Float (default: 0)
#scale-blur

# Float (0 to 1) (default: 0)
#scale-clamp

# Float (default: default)
#scale-param1

# Float (default: default)
#scale-param2

# Float (0.5 to 16) (default: 0)
#scale-radius

# Float (0 to 1) (default: 0)
#scale-taper

# bartlett cosine hanning tukey hamming quadric welch kaiser blackman sphinx jinc (default: )
#scale-window                   Choices:

# Float (default: default)
#scale-wparam

# Float (0 to 1) (default: 0)
#scale-wtaper

# Flag (default: yes)
#scaler-resizes-only

# Choices: default (or an integer) (0 to 32) (default: default)
#screen

# String (default: )
#screen-name

# String (default: libaom-av1)
#screenshot-avif-encoder

# Key/value list (default: usage=allintra,crf=0,cpu-used=8)
#screenshot-avif-opts

# String (default: )
#screenshot-avif-pixfmt

# String (default: ) [file]
#screenshot-dir

# alias for screenshot-dir
#screenshot-directory

# Choices: jpg jpeg png webp jxl avif (default: jpg)
#screenshot-format

# Flag (default: yes)
#screenshot-high-bit-depth

# Integer (0 to 100) (default: 90)
#screenshot-jpeg-quality

# Flag (default: yes)
#screenshot-jpeg-source-chroma

# Double (0 to 15) (default: 1)
#screenshot-jxl-distance

# Integer (1 to 9) (default: 4)
#screenshot-jxl-effort

# Integer (0 to 9) (default: 7)
#screenshot-png-compression

# Integer (0 to 5) (default: 5)
#screenshot-png-filter

# Flag (default: no)
#screenshot-sw

# Flag (default: yes)
#screenshot-tag-colorspace

# String (default: mpv-shot%n)
#screenshot-template

# Integer (0 to 6) (default: 4)
#screenshot-webp-compression

# Flag (default: no)
#screenshot-webp-lossless

# Integer (0 to 100) (default: 75)
#screenshot-webp-quality

# alias for --scripts-append (CLI/config files only)
#script

# alias for --script-opts-append (CLI/config files only)
#script-opt

# Key/value list (default: )
#script-opts

# String list
#scripts

# Float (default: 0)
#sdl-buflen

# Flag (default: no)
#sdl-sw

# Flag (default: no)
#sdl-switch-mode

# Flag (default: yes)
#sdl-vsync

# Choices: auto yes no (default: auto)
#sdr-adjust-gamma

# Choices: no auto (or an integer) (0 to 8190) (default: no)
#secondary-sid

# Choices: no yes scale force strip (default: strip)
#secondary-sub-ass-override

# Float (default: 0)
#secondary-sub-delay

# Float (0 to 150) (default: 0)
#secondary-sub-pos

# Flag (default: yes)
#secondary-sub-visibility

# Float (default: 0)
#sharpen

# Flag (default: yes)
#show-in-taskbar

# Flag (default: no)
#shuffle

# Choices: no auto (or an integer) (0 to 8190) (default: auto)
#sid

# Float (0 to 1) (default: 0.75)
#sigmoid-center

# Float (1 to 20) (default: 6.5)
#sigmoid-slope

# Flag (default: yes)
#sigmoid-upscaling

# String list (default: )
#slang

# Flag (default: no)
#snap-window

# Double (0.01 to 100) (default: 1)
#speed

# Choices: auto (default: auto)
#spirv-compiler

# Double (0 to any) (default: 0)
#sstep

# Relative time or percent position (default: none)
#start

# Flag (default: no)
#stop-playback-on-init-failure

# Choices: no yes always (default: yes)
#stop-screensaver

# ByteSize (4096 to 536870912) (default: 128.000 KiB)
#stream-buffer-size

# String (default: ) [file]
#stream-dump

# Key/value list (default: )
#stream-lavf-o

# String (default: )
#stream-record

# Flag (default: no)
#stretch-dvd-subs

# Flag (default: no)
#stretch-image-subs-to-screen

# alias for sid
#sub

# Choices: left center right (default: center)
#sub-align-x

# Choices: top center bottom (default: bottom)
#sub-align-y

# Flag (default: yes)
#sub-ass

# Flag (default: no)
#sub-ass-force-margins

# alias [deprecated] for sub-ass-style-overrides
#sub-ass-force-style

# alias [deprecated] for sub-hinting
#sub-ass-hinting

# Flag (default: no)
#sub-ass-justify

# alias [deprecated] for sub-line-spacing
#sub-ass-line-spacing

# Choices: no yes scale force strip (default: scale)
#sub-ass-override

# Double (-1 to 10000) (default: -1)
#sub-ass-prune-delay

# Flag (default: no)
#sub-ass-scale-with-window

# alias [deprecated] for sub-shaper
#sub-ass-shaper

# String list (default: )
#sub-ass-style-overrides

# String (default: ) [file]
#sub-ass-styles

# Choices: none aspect-ratio all (default: all)
#sub-ass-use-video-data

# Aspect (0 to 10) (default: 0)
#sub-ass-video-aspect-override
#sub-ass-vsfilter-aspect-compat removed [deprecated]

# removed [deprecated]
#sub-ass-vsfilter-blur-compat

# Choices: no basic full force-601 (default: basic)
#sub-ass-vsfilter-color-compat

# Choices: no exact fuzzy all (default: exact)
#sub-auto

# String list (default: ass,idx,lrc,mks,pgs,rt,sbv,scc,smi,srt,ssa,sub,sup,utf,utf-8,utf8,vtt)
#sub-auto-exts

# Color (default: #AF000000)
#sub-back-color

# Integer (default: 0)
#sub-bitmap-max-size

# Float (0 to 20) (default: 0)
#sub-blur

# Flag (default: no)
#sub-bold

# alias for sub-outline-color
#sub-border-color

# alias for sub-outline-size
#sub-border-size

# Choices: outline-and-shadow opaque-box background-box (default: outline-and-shadow)
#sub-border-style

# Flag (default: no)
#sub-clear-on-seek

# String (default: auto)
#sub-codepage

# Color (default: #FFFFFFFF)
#sub-color

# Flag (default: no)
#sub-create-cc-track

# Float (default: 0)
#sub-delay

# String (default: )
#sub-demuxer

# alias for --sub-files-append (CLI/config files only)
#sub-file

# String list
#sub-file-paths

# String list
#sub-files

# String list (default: )
#sub-filter-jsre

# String list (default: )
#sub-filter-regex

# Flag (default: yes)
#sub-filter-regex-enable

# Flag (default: no)
#sub-filter-regex-plain

# Flag (default: no)
#sub-filter-regex-warn

# Flag (default: no)
#sub-filter-sdh

# String list,（）)
#sub-filter-sdh-enclosures

# Flag (default: no)
#sub-filter-sdh-harder

# Flag (default: no)
#sub-fix-timing

# Integer (default: 400)
#sub-fix-timing-keep

# Integer (default: 210)
#sub-fix-timing-threshold

# String (default: sans-serif)
#sub-font

# Choices: auto none fontconfig (default: auto)
#sub-font-provider

# Float (1 to 9000) (default: 38)
#sub-font-size

# String (default: ) [file]
#sub-fonts-dir

# Flag (default: no)
#sub-forced-events-only

# Float (default: 0)
#sub-fps

# Float (0 to 3) (default: 0)
#sub-gauss

# Integer (default: 0)
#sub-glyph-limit

# Flag (default: no)
#sub-gray

# Choices: sdr (or an integer) (10 to 10000) (default: sdr)
#sub-hdr-peak

# Choices: none light normal native (default: none)
#sub-hinting

# Flag (default: no)
#sub-italic

# Choices: auto left center right (default: auto)
#sub-justify

# Key/value list (default: )
#sub-lavc-o

# Float (-1000 to 1000) (default: 0)
#sub-line-spacing

# Integer (0 to 2147483647) (default: 19)
#sub-margin-x

# Integer (0 to 2147483647) (default: 34)
#sub-margin-y

# Color (default: #FF000000)
#sub-outline-color

# Float (default: 1.65)
#sub-outline-size

# Flag (default: no)
#sub-past-video-end

# Float (0 to 150) (default: 100)
#sub-pos

# Float (0 to 100) (default: 1)
#sub-scale

# Flag (default: yes)
#sub-scale-by-window

# Flag (default: no)
#sub-scale-signs

# Flag (default: yes)
#sub-scale-with-window

# alias for sub-back-color
#sub-shadow-color

# Float (default: 0)
#sub-shadow-offset

# Choices: simple complex (default: complex)
#sub-shaper

# Float (-10 to 10) (default: 0)
#sub-spacing

# Float (default: 1)
#sub-speed

# Flag (default: no)
#sub-stretch-durations

# Flag (default: yes)
#sub-use-margins

# Flag (default: yes)
#sub-visibility

# Flag (default: no)
#sub-vsfilter-bidi-compat

# Choices: no default yes (default: default)
#subs-fallback

# Choices: no yes always (default: yes)
#subs-fallback-forced

# Flag (default: yes)
#subs-match-os-language

# Choices: no forced yes (default: yes)
#subs-with-matching-audio

# Integer (1 to 8) (default: 2)
#swapchain-depth

# Flag (default: yes)
#sws-allow-zimg

# Flag (default: no)
#sws-bitexact

# Float (0 to 100) (default: 0)
#sws-cgb

# Integer (-100 to 100) (default: 0)
#sws-chs

# Float (-100 to 100) (default: 0)
#sws-cs

# Integer (-100 to 100) (default: 0)
#sws-cvs

# Flag (default: no)
#sws-fast

# Float (0 to 100) (default: 0)
#sws-lgb

# Float (-100 to 100) (default: 0)
#sws-ls

# Choices: (default: lanczos)
#   fast-bilinear bilinear bicubic x point area bicublin gauss sinc lanczos
#   spline
#sws-scaler

# Choices: auto no yes (default: auto)
#target-colorspace-hint

# Choices: target source source-dynamic (default: target)
#target-colorspace-hint-mode

# Flag (default: yes)
#target-colorspace-hint-strict

# Choices: auto inf (or an integer) (10 to 10000000) (default: auto)
#target-contrast

# Choices: (default: auto)
#   auto bt.601-525 bt.601-625 bt.709 bt.2020 bt.470m apple adobe prophoto
#   cie1931 dci-p3 display-p3 v-gamut s-gamut ebu3213 film-c aces-ap0 aces-ap1
#target-gamut

# String (default: ) [file]
#target-lut

# Choices: auto (or an integer) (10 to 10000) (default: auto)
#target-peak

# Choices: auto bt.601-525 bt.601-625 bt.709 bt.2020 bt.470m apple adobe prophoto cie1931 dci-p3 display-p3 v-gamut s-gamut ebu3213 film-c aces-ap0 aces-ap1 (default: auto)
#target-prim

# Choices: auto bt.1886 srgb linear gamma1.8 gamma2.0 gamma2.2 gamma2.4 gamma2.6 gamma2.8 prophoto pq hlg v-log s-log1 s-log2 st428 (default: auto)
#target-trc

# Flag (default: yes)
#taskbar-progress

# Integer (-1 to 999) (default: 0)
#teletext-page

# Flag (default: no)
#temporal-dither

# Integer (1 to 128) (default: 1)
#temporal-dither-period

# Choices: force auto no (default: auto)
#term-osd

# Flag (default: no)
#term-osd-bar

# String (default: [-+-])
#term-osd-bar-chars

# String (default: )
#term-playing-msg

# String (default: )
#term-status-msg

# String (default: )
#term-title

# Flag (default: yes)
#terminal

# String (default: ${?media-title:${media-title}}${!media-title:No file} - mpv)
#title

# Flag (default: yes)
#title-bar

# String (default: ) [file]
#tls-ca-file

# String (default: ) [file]
#tls-cert-file

# String (default: ) [file]
#tls-key-file

# Flag (default: no)
#tls-verify

# Choices: auto clip mobius reinhard hable gamma linear spline bt.2390 bt.2446a st2094-40 st2094-10 (default: auto)
#tone-mapping

# removed [deprecated]
#tone-mapping-desaturate
#tone-mapping-desaturate-exponent removed [deprecated]

# Float (1 to 10) (default: 1)
#tone-mapping-max-boost

# Float (default: default)
#tone-mapping-param

# Flag (default: no)
#tone-mapping-visualize

# Flag (default: yes)
#track-auto-selection

# Choices: no input output both auto (default: auto)
#treat-srgb-as-power22

# Choices: oversample linear spline16 spline36 spline64 sinc lanczos ginseng bicubic hermite catmull_rom mitchell robidoux robidouxsharp box nearest triangle gaussian bartlett cosine hanning tukey hamming quadric welch kaiser blackman sphinx jinc (default: oversample)
#tscale

# Float (0 to 1) (default: 0)
#tscale-antiring

# Float (default: 0)
#tscale-blur

# Float (0 to 1) (default: 0)
#tscale-clamp

# Float (default: default)
#tscale-param1

# Float (default: default)
#tscale-param2

# Float (0.5 to 16) (default: 0)
#tscale-radius

# Float (0 to 1) (default: 0)
#tscale-taper

# bartlett cosine hanning tukey hamming quadric welch kaiser blackman sphinx jinc (default: )
#tscale-window                  Choices:

# Float (default: default)
#tscale-wparam

# Float (0 to 1) (default: 0)
#tscale-wtaper

# Flag (default: no)
#untimed

# Flag (default: yes)
#use-embedded-icc-profile

# Flag (default: no)
#use-filedir-conf

# String (default: libmpv)
#user-agent

# String (default: /dev/dri/renderD128)
#vaapi-device

# String (default: )
#vd

# Flag (default: yes)
#vd-apply-cropping

# Flag (default: no)
#vd-lavc-assume-old-x264

# Flag (default: no)
#vd-lavc-bitexact

# Flag (default: yes)
#vd-lavc-check-hw-profile

# Choices: auto no yes (default: auto)
#vd-lavc-dr

# Flag (default: no)
#vd-lavc-fast

# Choices: auto cpu gpu (default: auto)
#vd-lavc-film-grain

# Choices: none default nonref bidir nonkey all (default: nonref)
#vd-lavc-framedrop

# Key/value list (default: )
#vd-lavc-o

# Flag (default: no)
#vd-lavc-show-all

# Choices: none default nonref bidir nonkey all (default: default)
#vd-lavc-skipframe

# Choices: none default nonref bidir nonkey all (default: default)
#vd-lavc-skipidct

# Choices: none default nonref bidir nonkey all (default: default)
#vd-lavc-skiploopfilter

# alias [deprecated] for hwdec-software-fallback
#vd-lavc-software-fallback

# Integer (0 to any) (default: 0)
#vd-lavc-threads

# Flag (default: no)
#vd-queue-enable

# ByteSize (0 to 4.6116860184274e+18) (default: 512.000 MiB)
#vd-queue-max-bytes

# Integer64 (0 to any) (default: 50)
#vd-queue-max-samples

# Double (0 to any) (default: 2)
#vd-queue-max-secs

# Object settings list (default: )
#vf

# Choices: no auto (or an integer) (0 to 8190) (default: auto)
#vid

# alias for vid
#video

# Float (-1 to 1) (default: 0)
#video-align-x

# Float (-1 to 1) (default: 0)
#video-align-y

# Choices: bitstream container ignore (default: container)
#video-aspect-method

# Aspect (-2 to 10) (default: -2)
#video-aspect-override

# Integer (0 to 1024) (default: 1)
#video-backward-batch

# Choices: auto (or an integer) (0 to 1024) (default: auto)
#video-backward-overlap

# Video rect (default: )
#video-crop

# String list (default: 3g2,3gp,avi,flv,ivf,m2ts,m4v,mj2,mkv,mov,mp4,mpeg,mpg,mxf,ogv,rmvb,ts,webm,wmv,y4m)
#video-exts

# Flag (default: no)
#video-latency-hacks

# Float (0 to 1) (default: 0)
#video-margin-ratio-bottom

# Float (0 to 1) (default: 0)
#video-margin-ratio-left

# Float (0 to 1) (default: 0)
#video-margin-ratio-right

# Float (0 to 1) (default: 0)
#video-margin-ratio-top

# Flag (default: yes)
#video-osd

# Choices: auto limited full (default: auto)
#video-output-levels

# Float (default: 0)
#video-pan-x

# Float (default: 0)
#video-pan-y

# Flag (default: no)
#video-recenter

# ByteSize (0 to 4.6116860184274e+18) (default: 1.000 GiB)
#video-reversal-buffer

# Choices: no (or an integer) (0 to 359) (default: 0)
#video-rotate

# Float (0 to 10000) (default: 1)
#video-scale-x

# Float (0 to 10000) (default: 1)
#video-scale-y

# Choices: audio display-resample display-resample-vdrop display-resample-desync display-tempo display-adrop display-vdrop display-desync desync (default: audio)
#video-sync

# Double (0 to 1) (default: 0.125)
#video-sync-max-audio-change

# Integer (1 to 10) (default: 5)
#video-sync-max-factor

# Double (0 to any) (default: 1)
#video-sync-max-video-change

# Double (0 to 1) (default: 0.05)
#video-timing-offset

# Choices: no yes downscale-big (default: no)
#video-unscaled

# Float (-20 to 20) (default: 0)
#video-zoom

# String list (default: )
#vlang

# Object settings list (default: )
#vo

# String (default: libaom-av1)
#vo-image-avif-encoder

# Key/value list (default: usage=allintra,crf=0,cpu-used=8)
#vo-image-avif-opts

# String (default: )
#vo-image-avif-pixfmt

# Choices: jpg jpeg png webp jxl avif (default: jpg)
#vo-image-format

# Flag (default: yes)
#vo-image-high-bit-depth

# Integer (0 to 100) (default: 90)
#vo-image-jpeg-quality

# Flag (default: yes)
#vo-image-jpeg-source-chroma

# Double (0 to 15) (default: 1)
#vo-image-jxl-distance

# Integer (1 to 9) (default: 4)
#vo-image-jxl-effort

# String (default: ) [file]
#vo-image-outdir

# Integer (0 to 9) (default: 7)
#vo-image-png-compression

# Integer (0 to 5) (default: 5)
#vo-image-png-filter

# Flag (default: yes)
#vo-image-tag-colorspace

# Integer (0 to 6) (default: 4)
#vo-image-webp-compression

# Flag (default: no)
#vo-image-webp-lossless

# Integer (0 to 100) (default: 75)
#vo-image-webp-quality

# Flag (default: yes)
#vo-kitty-alt-screen
#vo-kitty-auto-multiplexer-passthrough Flag (default: no)

# Integer (default: 0)
#vo-kitty-cols

# Flag (default: yes)
#vo-kitty-config-clear

# Integer (default: 0)
#vo-kitty-height

# Integer (default: 0)
#vo-kitty-left

# Integer (default: 0)
#vo-kitty-rows

# Integer (default: 0)
#vo-kitty-top

# Flag (default: no)
#vo-kitty-use-shm

# Integer (default: 0)
#vo-kitty-width

# Double (0 to 10000) (default: 0)
#vo-null-fps

# Flag (default: yes)
#vo-sixel-alt-screen

# Flag (default: no)
#vo-sixel-buffered

# Integer (default: 0)
#vo-sixel-cols

# Flag (default: yes)
#vo-sixel-config-clear

# Choices: auto none atkinson fs jajuni stucki burkes arithmetic xor (default: auto)
#vo-sixel-dither

# Flag (default: yes)
#vo-sixel-fixedpalette

# Integer (default: 0)
#vo-sixel-height

# Integer (default: 0)
#vo-sixel-left

# Integer (default: -1)
#vo-sixel-pad-x

# Integer (default: -1)
#vo-sixel-pad-y

# Integer (default: 256)
#vo-sixel-reqcolors

# Integer (default: 0)
#vo-sixel-rows

# Integer (default: -1)
#vo-sixel-threshold

# Integer (default: 0)
#vo-sixel-top

# Integer (default: 0)
#vo-sixel-width

# Flag (default: no)
#vo-tct-256

# Choices: plain half-blocks (default: half-blocks)
#vo-tct-algo

# Choices: pixel line frame (default: line)
#vo-tct-buffering

# Integer (default: 0)
#vo-tct-height

# Integer (default: 0)
#vo-tct-width

# Flag (default: no)
#vo-vaapi-scaled-osd

# Choices: default fast hq nla (default: default)
#vo-vaapi-scaling

# Flag (default: yes)
#vo-vdpau-chroma-deint

# Color (default: #FF020507)
#vo-vdpau-colorkey

# Flag (default: yes)
#vo-vdpau-composite-detect

# Float (0 to 1) (default: 0)
#vo-vdpau-denoise

# Flag (default: no)
#vo-vdpau-force-yuv

# Double (default: 0)
#vo-vdpau-fps

# Integer (0 to 9) (default: 0)
#vo-vdpau-hqscaling

# Integer (2 to 15) (default: 3)
#vo-vdpau-output-surfaces

# Flag (default: no)
#vo-vdpau-pullup

# Integer (default: 50)
#vo-vdpau-queuetime-fs

# Integer (default: 50)
#vo-vdpau-queuetime-windowed

# Float (-1 to 1) (default: 0)
#vo-vdpau-sharpen

# Float (-1 to 1000) (default: 100)
#volume

# Float (-150 to 150) (default: 0)
#volume-gain

# Float (0 to 150) (default: 12)
#volume-gain-max

# Float (-150 to 0) (default: -96)
#volume-gain-min

# Float (100 to 1000) (default: 130)
#volume-max

# Flag (default: yes)
#vulkan-async-compute

# Flag (default: yes)
#vulkan-async-transfer

# String (default: )
#vulkan-device

# Integer (default: 0)
#vulkan-display-display

# Integer (default: 0)
#vulkan-display-mode

# Integer (default: 0)
#vulkan-display-plane

# Integer (1 to 8) (default: 1)
#vulkan-queue-count

# Choices: auto fifo fifo-relaxed mailbox immediate (default: auto)
#vulkan-swap-mode

# String (default: ~~state/watch_history.jsonl) [file]
#watch-history-path

# String (default: ) [file]
#watch-later-dir

# alias for watch-later-dir
#watch-later-directory

# String list (default: start,speed,pitch,edition,volume,mute,audio-delay,gamma,brightness,contrast,saturation,hue,deinterlace,vf,af,panscan,aid,vid,sid,sub-delay,sub-speed,sub-pos,sub-visibility,sub-scale,sub-use-margins,sub-ass-force-margins,sub-ass-use-video-data,sub-ass-override,secondary-sid,secondary-sub-delay,secondary-sub-pos,secondary-sub-ass-override,secondary-sub-visibility,ab-loop-a,ab-loop-b,video-aspect-override,video-aspect-method,video-unscaled,video-pan-x,video-pan-y,video-rotate,video-crop,video-zoom,video-scale-x,video-scale-y,video-align-x,video-align-y)
#watch-later-options

# String (default: mpv)
#wayland-app-id

# Choices: auto no yes (default: auto)
#wayland-configure-bounds

# Choices: auto none photo video game (default: auto)
#wayland-content-type

# Flag (default: no) [deprecated]
#wayland-disable-vsync

# Integer (0 to 2147483647) (default: 16)
#wayland-edge-pixels-pointer

# Integer (0 to 2147483647) (default: 32)
#wayland-edge-pixels-touch

# Choices: no auto yes (default: auto)
#wayland-internal-vsync

# Flag (default: yes)
#wayland-present

# Integer64 (default: -1)
#wid

# Flag (default: yes)
#window-dragging

# Flag (default: no)
#window-maximized

# Flag (default: no)
#window-minimized

# Double (0.001 to 100) (default: 1)
#window-scale
#write-filename-in-watch-later-config Flag (default: no)

# Choices: no yes fs-only never (default: fs-only)
#x11-bypass-compositor

# String (default: )
#x11-name

# Choices: auto no yes (default: auto)
#x11-netwm

# Choices: no auto yes (default: auto)
#x11-present

# Flag (default: no)
#x11-wid-title

# Integer (-1 to any) (default: -1)
#xv-adaptor

# Integer (1 to 10) (default: 2)
#xv-buffers

# Choices: use set cur (default: cur)
#xv-ck

# Choices: none bg man auto (default: man)
#xv-ck-method

# Integer (default: 65280)
#xv-colorkey

# Integer (0 to any) (default: 0)
#xv-port

# Flag (default: yes)
#ytdl

# String (default: )
#ytdl-format

# Key/value list (default: )
#ytdl-raw-options

# Choices: no ordered random error-diffusion (default: random)
#zimg-dither

# Flag (default: yes)
#zimg-fast

# Choices: point bilinear bicubic spline16 spline36 lanczos (default: lanczos)
#zimg-scaler

# Choices: point bilinear bicubic spline16 spline36 lanczos (default: bilinear)
#zimg-scaler-chroma

# Double (default: default)
#zimg-scaler-chroma-param-a

# Double (default: default)
#zimg-scaler-chroma-param-b

# Double (default: default)
#zimg-scaler-param-a

# Double (default: default)
#zimg-scaler-param-b

# Choices: auto (or an integer) (1 to 64) (default: auto)
#zimg-threads

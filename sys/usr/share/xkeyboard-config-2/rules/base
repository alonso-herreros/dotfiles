// Add these lines under the '! option = symbols' section. If it doesn't exist, create it.

  altgreek                       = +altgreek(basic)
  altgreek:mathvar               = +altgreek(mathvar)
  altmath                        = +altmath(basic)
  altmath:sub_comma              = +altmath(sub_comma)
  altmath:sup_dot                = +altmath(sup_dot)
  undead:es_only                 = +undead(es_only)
  numpad                         = +numpad(basic)
  numpad:hex                     = +numpad(hex)
  numpad:operators_ext           = +numpad(operators_ext)
  custom:types                   = +custom(types)
  custom:test                    = +custom(test)


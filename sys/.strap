#!/usr/bin/env bash

install-all() {
    dir="$1"
    prefix="$2"
    sudo find -L "$dir" -type f ! -exec diff -u --color=always "/{}" "{}" \; -ok install -Dm644 "{}" -T "$prefix/{}" \;
}

install-all etc
install-all usr/lib/systemd
